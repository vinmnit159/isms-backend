# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/isms_db"
# Railway will automatically set DATABASE_URL environment variable

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Server Configuration
NODE_ENV="development"
PORT=3000
HOST="0.0.0.0"
# Railway will automatically set PORT environment variable

# CORS Configuration
CORS_ORIGIN="http://localhost:5173"
# For production, update to your frontend domain: "https://your-frontend-domain.vercel.app"

# Logging Configuration
LOG_LEVEL="info"

# Railway-specific environment variables (set these in Railway dashboard):
# - DATABASE_URL: Automatically provided by Railway PostgreSQL service
# - PORT: Automatically provided by Railway (usually 3000)
# - NODE_ENV: Set to "production" for production deployments
# - JWT_SECRET: Generate a strong secret key for production
# - CORS_ORIGIN: Set to your frontend application URL

# Optional: Database seeding (set to "true" to seed the database)
# SEED_DATABASE="false"

# Google OAuth (set these in Railway dashboard for production)
GOOGLE_CLIENT_ID="872942690819-s0v2lsqq9lrk3i3hcpv9hvpe9bffc4h9.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="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"
# Must exactly match the redirect URI registered in Google Cloud Console
GOOGLE_CALLBACK_URL="https://ismsbackend.bitcoingames1346.com/auth/google/callback"
# Where the backend redirects after successful Google sign-in
FRONTEND_URL="https://isms.bitcoingames1346.com"